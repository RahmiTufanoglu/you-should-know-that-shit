import { BackendValidationPipe } from './backend-validation.pipe';

describe('BackendValidationPipe', () => {
  it('should be defined', () => {
    expect(new BackendValidationPipe()).toBeDefined();
  });
});
